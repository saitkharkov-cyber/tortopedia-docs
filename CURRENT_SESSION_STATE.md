# CURRENT SESSION STATE

Дата: 2026-09-15
Проект: Tortopedia
Основной сайт: `D:\Git\tortopedia`
Документация после локального переноса: `D:\Git\tortopedia\docs`
Ветка обоих репозиториев: `main`
Последний коммит сайта: `623aec2 Add fondant figurine storage article`

## Сейчас сделано

- Статья «Как хранить фигурки из мастики» подготовлена RU/UA, собрана и отправлена в `main`.
- Push сайта выполнен успешно.
- Категории, сайдбары, HTML sitemap и `sitemap.xml` обновлены.
- Введена новая схема документации:
  - `START_SESSION.md` — единственная точка входа;
  - `CHATGPT_WORKFLOW_RULES.md` читается после него обязательно;
  - `CURRENT_SESSION_STATE.md` регулярно поддерживается во время длинной активной сессии;
  - `HANDOFF.md` используется только для передачи контекста следующему чату/после длительной паузы.
- `CHATGPT_WORKFLOW_RULES.md`, `CHATGPT_WORKFLOW_REFERENCE.md`, `HANDOFF.md` и `TORTOPEDIA_CONTENT_STYLE_GUIDE.md` приведены к новой схеме.
- Создан `START_SESSION.md`.

## Текущее состояние Git

Удалённый сайт:

```text
saitkharkov-cyber/tortopedia @ 623aec2
```

Удалённая документация изменена в GitHub в этой сессии. Пользователю ещё нужно синхронизировать её локально.

## Решения этой сессии

1. Любой новый рабочий чат начинается только с `START_SESSION.md`.
2. HANDOFF больше не содержит общий стартовый алгоритм, а только напоминает начать с `START_SESSION.md`.
3. ChatGPT обязан сам регулярно отслеживать необходимость обновления `CURRENT_SESSION_STATE.md`.
4. Документация локально переносится внутрь дерева сайта в `D:\Git\tortopedia\docs`.
5. Папка `docs/` должна игнорироваться основным репозиторием сайта и оставаться отдельным вложенным Git-репозиторием документации.
6. Git сайта и Git документации нельзя смешивать.

## Что намеренно не трогаем

- Не объединяем Git-истории сайта и документации в один репозиторий.
- Не включаем документацию в production-сборку сайта.
- Не начинаем следующую контентную статью до завершения синхронизации документации и проверки публикации текущей статьи.

## Текущая задача

Завершить переход на новую структуру документации:

1. добавить `docs/` в `.gitignore` основного сайта;
2. синхронизировать оба локальных репозитория;
3. перенести локальный `tortopedia-docs` внутрь `D:\Git\tortopedia\docs`;
4. проверить чистый Git обоих репозиториев.

После этого проверить боевые RU/UA URL новой статьи, `sitemap.xml` и отправить обе страницы в GSC.

## Следующий единственный шаг

После того как изменения `.gitignore` будут отправлены в GitHub, пользователь может выполнить pull обоих репозиториев, после чего безопасно перенести локальную документацию внутрь сайта.
